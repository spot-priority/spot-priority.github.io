# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## Common Commands

- **View local documentation**: Open the site in a browser.
  ```bash
  open docs/index.html
  ```
- **Lint Markdown** (if installed):
  ```bash
  markdownlint --config .markdownlint.json **/*.md
  ```
- **Run a local static server** (optional, if you want to see changes live):
  ```bash
  npx http-server . -p 8000
  ```
- **Run tests**: No automated tests are defined for this repository.

## Build Process

The site is static HTML/CSS/JS. No build step is required beyond editing files. For a quick preview, start a local HTTP server as shown above.

## High‑Level Architecture

- **Content**: Written in Markdown and compiled to HTML in the `docs` folder. The main documentation is under `docs/pages`.
- **UI Components**: Basic CSS in `docs/styles.css` and JavaScript for interactive elements in `docs/tool/`.
- **Drag‑and‑Drop**: Implemented by `priority‑tool‑drag.js` which manages task lists on the page.
- **Static assets**: Images in `docs/images`, PDF in `docs/pdf`.
- **Deployment**: Hosted on GitHub Pages (`https://spot-priority.github.io/`).

## Key Files & Directories

- `docs/index.html` – entry point for the documentation site.
- `docs/pages/*` – individual page HTML files.
- `docs/tool/priority-tool-drag.js` – JavaScript for drag‑and‑drop functionality.
- `README.md` – project overview.
- `LICENSE.md` – licensing information.

## Contributing

Feel free to fork and submit PRs. The repository contains no CI/CD or linting configuration by default, but you may add them if desired.

---

*Generated by Claude Code*